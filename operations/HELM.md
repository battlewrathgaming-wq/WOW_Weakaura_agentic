# HELM — who has the trunk

_Do NOT read this by eye — run `py operations/boot.py --lane <yours>` (PROTOCOL.md §2): it reads this, diagnoses the condition, and says why the trunk moved. Rules: §3 + appendix · custody: `git log -- operations/HELM.md` · history: operations/archive/_

holder:  addons
since:   2026-08-17
heading: ROUTER correction (H0-b overrules "never compute your own") + the four queued desk items — ts through reduce_run, B3 closed, the 5.5 write-up, the monotone-approach line. W1/W5 held for the acceptance route.
runway:  short — release when the desk items land
