# Audit B — MODEL vs ARC, section by section

_Independent audit, 2026-08-17. Files only; no thread context. Question: for each section of
`dungeonrun_model.md`, what did the 2026-08-17 driver-analysis arc do to it._

**Inputs read in full:** `dungeonrun_model.md` (1,611 lines) · `mvp_scope.md` · `driver_analysis_brief.md` ·
`driver_analysis_asklist.md` · `driver_design_advisory.md` · `driver_walk_acceptance.md` ·
`driver_walk_result.md` · `driver_posture.md`.

**Cite keys:** `model L#` line in the model · `brief §N` · `ask §X / H#` (asklist section / theory-back
item) · `adv §N` (advisory) · `acc W#` (acceptance) · `res L#` (walk result line) · `post §N` (posture).

**Verdicts:** STANDS · SHRINKS · ANSWERED-BY-DATA · CONTRADICTED · OPEN · UNTOUCHED. Where a section is
only partly touched the row is split (a/b/c). A CONTRADICTED row shows both sides and decides nothing.

---

## B1. Delta table — one row per model section, in the model's order

| # | model § (line) | model claims | arc finding (cite) | verdict | reason |
|---|---|---|---|---|---|
| 1a | The mission (L18) — route emitted from play; pulls are the route | a route is emitted from capture, never placed | adv §12 "SEED ONCE… no way to make a beacon that did not come from a read"; acc W0 "No dataset → no beacon" | STANDS | arc re-states the origin gate as the walk's and package's law |
| 1b | ★★ We build the instrument, not the expertise (L29) — never tell the user what a good route IS | boundary: hold what happened, hand over means to see it; helpfulness crosses it | brief §5 bounds carry it verbatim; adv §10 "desk readout… never a warning ('looks like helpfulness on the way over')"; adv R-c no on-screen pack count; ask H15 "is this becoming a model of combat? YES; corrected"; res L469-475 scope fence in walk.py | STANDS | re-asserted; arc drifted toward combat/physics modelling twice and pulled back to it (ask H15, res L464-467) |
| 2a | ★★★ The addon occupies only what the user opens (L53) | non-invasive; UI occupation user-driven | ask F-ii "route driver re-points every stage… 'reclaim' under Landmarks' contract… needs its own stated answer to when do we give it back"; ask §I OPEN — Battlewrath's rulings; adv §8 not decided | OPEN | invasiveness question of the tracker slot raised by the arc, not ruled |
| 2b | same — occupation (L53) | as above | acc W6 L197-203 "Release is a REQUIREMENT… we are the only actor that can [clear]"; res L494-501 `ts=0` at every clear; W6 6 set/6 clear | ANSWERED-BY-DATA | terminal release measured as needed and working; a route ending armed leaves a live-looking stale arrow |
| 2c | THE ENTRY IS A SURFACE, NOT A COMMAND (L70) | slash surface is power path, not a door | ask §K G3 route remote "arm / go / stop / report… no typed commands" | STANDS | carried into the driver's surface list (via mvp_scope) |
| 3a | Capture is the only spawn (L82) — origin gate; nothing from nothing | passive/deliberate capture; derived point = nobody stood there | adv §12 seed once, carry forever; ask H0-b RULED "height is never invented… z is always the z of a REAL sample" (R-b); acc W0 walk refuses non-sample xyz | STANDS | re-ruled and extended to height |
| 3b | same — the gate is on ORIGIN not POSITION; "we don't gate them" dragging (L96-102) | drag after origin is the user's; no gate on position | ask L331-336 R-b: author drag "PLANAR BY CONSTRUCTION… different height means picking a point that was sampled there"; adv §12 "'moving' is not a drag to a free point, it is re-seating onto another read; without samples the tool does not offer the operation" | CONTRADICTED | model: free drag after origin, ungated · R-b: drag is planar (xy free, z sampled) · adv §12: move = re-seat onto a read only. Three positions, shown |
| 3c | And a run is a SURFACE READ (L104) — sampler 1 Hz regardless | every sample = evidence of standability | ask H8 W4 "1 Hz ADEQUATE for R=5 placement (2.41 yd max)… do not go coarser"; res L73-75 shipping constant; res L375-385 W4 doubled inside pulls (4.95) | ANSWERED-BY-DATA | 1 Hz cadence justified numerically; combat splits the floor 2.2/4.95 |
| 4a | ★★★ THE WALK (L117) — sprite + timeline; the missing external check | walk = the disagreement for a self-owned trigger space; timeline is an emission | acc W0 outputs "stage timeline (stage, gt, cause), false advances, skipped stages"; res W5.3 timelines; acc W7.1 byte-equal timeline | STANDS | timeline emission built (desk); it is the W7 golden |
| 4b | sprite can walk a RUN's data → route test / convergence (L135-153) | route × run: are detectors where paths CONVERGE; discarded runs = adversarial corpus | ask E1 "Q3 convergence parked as driver concern; lives in the walk"; acc W5.5 cross-fixture; res L259-265 SFK_live legs × SFK_Run4 route 58 reached/14 hit/44 skip and vice-versa | ANSWERED-BY-DATA | the cross-run walk was run on the desk and numbers emitted; convergence (Q3) itself parked |
| 4c | ⚠⚠ runs IN-GAME, paced by `play`; offline = a SECOND IMPLEMENTATION, refused (L155-179) | offline evaluator re-implements trigger logic; drift; the real evaluator must run | acc W0 "the walk is the desk simulator that executes the driver's… rule offline… becomes the GOLDEN the Lua port must reproduce"; ask H9 "the DESK WALK… IS the logic, run in Python, and becomes the golden the Lua port must reproduce"; acc W7 "the desk is the reference"; adv §11 walk lives in the EDITOR tier; ask H16 "'simulator'… withdrawn" | CONTRADICTED | model: an offline walk is a second evaluator with nothing noticing drift · arc: built the offline walk (walk.py) as the reference and installed W7 byte-equal port fidelity as the noticing; adv §11 still places the user-facing walk in the editor |
| 4d | addon feature, not an instrument; count shown IN THE ADDON (L181-204) | user has no bench; the walk IS the interface; count = summary on the face | ask §K "Deferred BY DESIGN… the editor's walk (advisory §11)"; adv §11 "it lives where the author is - the authoring review tool"; post §7 "`hit` is the honest column"; res L250-257 stage inflated by skips | OPEN | in-addon walk deferred; desk readouts define what the count would be (hit, not stage) |
| 4e | "the corpus already exists for US — RFC 2 runs (99, 232), SFK 2 (315, 698)" (L200-202) | four real paths, build against real data day one | ask D1 four runs in git; post §10 `floor` absent on both RFC_run1/Run2; acc W5 L134-136 "three pre-regime RFC runs RETIRE from W5 (their holes bracket the kills) and MOVE to W1.10"; ask H14 pre-regime holes 43/69/125 s | SHRINKS | the RFC runs are unusable as transit fixtures and lack floor; corpus grew instead (test1, rfc_combat, chain: ask H8, res L305, res L479) |
| 4f | ★★★ ROUTE TRAVELS; RUNS STAY HOME — "runs never need to travel, and the 317KB payload problem never arrives" (L206-227) | imported route × own runs is already the cross-person test | adv §12 "Three economies… DATASET its own string, its own import/export… DATASET strings carry the weight (~1000 x 7 fields), compress well"; adv §7 withdrawn "Embedding datasets in route packages -> datasets are their own economy" | CONTRADICTED | model: runs never travel · adv §12: datasets travel as their own economy (for spawn/re-seat by an importer), route package references by hash. Purposes differ (walk vs authoring); the "never travel" consequence is the disputed line |
| 4g | store holds LEGS → test is segment-against-reach (L229-232) | sampled run; segments not points | ask H0-a segment test; ask H4 rule; acc W1.1/W1.5/W1.6; res L127-134 closed form reproduced, W1.5 0 violations; acc W1.10 gap bound "two valid same-map samples far apart in time… are a HOLE, not a step"; ask H14 posture §3 retraction (segment was bridging 125 s holes) | SHRINKS | segment test built and proven; but not every stored leg is a valid segment — holes must break the chain (W1.2/W1.10) |
| 4h | §113 cut `walk.lua`; "being named and designed" (L234-237) | walk to be a designed feature | adv §11 Battlewrath's definition ("train along a track… did you put a tunnel over it… PUSH-PULL FITMENT"); ask §K deferred | STANDS | named (adv §11); design deferred to after the consumer |
| 5a | ★★★ We inform. We do not act for the player (L241) | ours = what player KNOWS; not ours = gameplay input | brief §5 carries it; adv R-d "pointer is for TRAVEL; notes are for ACTION"; adv §10 pointer is a HEADING, "must never make a player feel they have to zig-zag" | STANDS | re-asserted; arc adds manners for the pointer |
| 5b | announce edge — chat describes, never instructs (L262-266) | announce is nearest edge | — | UNTOUCHED | arc has no chat/announce material |
| 6a | ★★★ Readout box TWO LIVES (L268) — driving: note on the beacon reached, authored by a stranger | one component, derived + authored text | adv §4 "Notes don't ping around because satellites latch once… Shared note slots are last-writer-wins… reads as the most recently REACHED thing"; adv §13 `A note "come here"`; ask C-1 "follow this note" as an instruction | STANDS | note is an instruction response; arc names the trivial arbitration (last-writer-wins) |
| 6b | ONE SENDER ON THE DRIVER; no ladder problem (L310-319) | nothing to arbitrate on the driver side | adv §4 as above | STANDS | consistent (unordered satellites, single slot, last writer) |
| 6c | focus drop-down = editorial (L292-300); output-only sink (L302); cursor/response split (L325-335); hover IDs / click holds (L354-386); three open questions (L388-420) | editor-side box design | — | UNTOUCHED | no arc material |
| 6d | sanitise at BOUNDARY (import), not render (L337-352) | escape once per route at import | adv §12 "on import the editor re-flattens `source` and diffs against the shipped `flat`" (integrity, not escaping); adv §11 flatten "DATA ONLY (no functions/closures)" | UNTOUCHED | integrity/diff is not the `|c |T |H` sanitisation; the rendering hole (L446-451 ☐) not addressed |
| 6e | ★★★ NO INPUT REACHES AN INTERPRETER (L422-454) — chat is an argument; no executor; rendering ☐ | text reaches a sink never a parser | adv §11 "Flatten contract: DATA ONLY (no functions/closures)… consumer never computes a default and never validates"; adv §12 consumer "checks two things only (schema_version, integrity hash)" | STANDS | consumer form is data-only by design; rendering hole still ☐ (see 6d) |
| 6f | announce consented, improper BY CONSTRUCTION (L456-486) | driver-side tick; author cannot reach the command | — | UNTOUCHED | no arc material |
| 7 | ★★★ We are the product, not the thing authored inside one (L490) | everyman governs what we EXPOSE, not how we BUILD | — | UNTOUCHED | (adv §11 "product is TWO addons" is a fence, not this) |
| 8a | ★★★ We read the game; the game does not notify us (L511) — capture is ARMED | no event to declare against; `arm` in our vocabulary | — | UNTOUCHED | capture arming not discussed |
| 8b | at playback it inverts: OUR trigger is internal state; client supplies POSITION and nothing else (L531-551) | reach/unseen/stage/role are our values | ask H0-b RULED "detection uses our OWN positions"; adv R-a; ask C-6 `ts` never in the acceptance condition | STANDS | ruled explicitly: position in, everything else ours; the tracker's reading demoted to calibration/arrow |
| 8c | ⚠⚠ THE COST: a wrong trigger of ours is a wrong MODEL, nothing outside us disagrees → test surface, walks (L553-557) | no external check | acc W7 port fidelity (byte-equal); ask H16 "What remains to prove is TRANSITIONS, and W7 grades them on the port"; ask H5/acc W2.2 divergence detector `|sd−od|` (an external check on POSITION only, 1,386/1,386 flagged) | STANDS | argument re-used to justify W7; arc adds an outside check for position (not for triggers) |
| 8d | arms on authoring panes are "how we made it" (L559-562) | not evidence | — | UNTOUCHED | — |
| 9 | Two lanes, chosen at promotion (L566) — for me / for a run | map marker vs route | — | UNTOUCHED | — |
| 10a | ★★★ A beacon is a THEATRE, not a point (L578) — anchor position is a LABEL POSITION; "nothing downstream may consume it" (L593-596) | anchor xyz not a fact; consumers ignore it | adv §3 "THEATER (has kids) the parent's position + R is the SCENE = the arm zone… Arm only while the theater is current AND the player is inside R"; adv §5 `scene: { xyz, R, band: open }`; adv §13 `B1 theater <read> 40 open — scene` | CONTRADICTED | model: anchor position consumed by nothing · adv: parent xyz+R is the driver's arm zone (a consumer). adv §3 also says "NEVER a pointer target" (agrees on that half) |
| 10b | anchor's demotion — draw/place/print; children carry the jobs (L585-596) | children = radius/waypoint/note | adv §3 child = trigger → actions (note, pointer, CLEU-on, complete); adv R-e first child IS the lure, defaulted; adv §3 "Only a child TYPED as a completor moves the stage" | STANDS | same delegation; different vocabulary (see 10c) |
| 10c | ★★★ Children ARE the everyman surface — detect / condition / instruct dropdowns (L598-620) | detect: shape·reach·reach.up·reach.down·unseen; condition: role·stage·stage match·ramp; instruct: action·target·outcome | adv §13 "every node is DEAF until told to listen… a child carries a position (+R, band) and its response — no detection method of its own… Nothing per-child to configure about HOW it is detected"; adv §4 `mode: once|while`, `requires:[ids]`; adv §13 ENTITIES (id parent kind pos R up down mode) + INSTRUCTIONS (A / Q); adv §7 withdrew `repeat` flag; adv §13 `A activate <childID>` | CONTRADICTED | model: per-child detect/condition/instruct vocabulary incl. `unseen`, `role`, `stage match`, `ramp`, `target` · adv §13: per-child = position + R + band + mode + actions + requires; detection is conferred by activation, no per-child detect config; `role` → lure/completor/satellite typing; `target` removed ("ONLY LURES"). Both are decompositions; they do not match |
| 10d | Children are sample-sourced and absolute x/y/z (L622-633) | child takes full xyz from any sample | adv §12 "every beacon CARRIES ITS OWN SEEDED xyz — copied from a surface read"; adv §13 "every pos is a COPIED READ"; R-b | STANDS | — |
| 11 | What a beacon ANSWERS (L637) — on-ramp/note/ratchet; childless anchor is its own satisfier | bare beacon gives all three | adv §3 "BEACON (childless) position + R ±H IS the satisfaction. Self-completing. On-enter lure is its own pointer (= v1)"; adv §5 childless compile target; adv §13 RULED "ONLY LURES — a beacon points to SELF… two radii… are two STEPS on one position" | STANDS | v1 form confirmed |
| 12 | ★★★ Curation is DISTINCTION (L653) — valuation across runs; filter vs generate; curate before promote | ranking, filtering, time window | adv §12 dataset consumers "replay watchers (the time-window playback)" | UNTOUCHED | one passing reference; no change |
| 13 | ★★★ Promotion is REDUCTION (L689) — route form dictated by consumer: beacons and stages | run → few beacons; instruction shape | ask H0-b RULED "the model is an INSTRUCTION SET, not detectors… driver is a dumb walker"; adv §0; adv §11 EDITOR owns the FLATTEN on save/compile; ask §K G1 "v1 reads the route structure directly (flatten deferred, per scope)" | STANDS | consumer-dictated form re-stated as instruction set; flatten deferred for v1 |
| 14a | The stage, and the ratchet (L712) — complete = max(index, outcome) | ratchet never walks backwards | adv §2 "forward listen current + K stages ahead (K = 3 default)… any forward stage satisfied -> jump; intermediates SKIPPED"; acc W5.2 K=all vs K=3; res L224-244 "K=all collapses the advance count as R grows" (SFK_Run4 R=8: 13 adv/8 false at K=all vs 54/6 at K=3) | ANSWERED-BY-DATA | `max(index, outcome)` = K=all; its false-advance cost measured; K bounds it (proposal) |
| 14b | `set` = N, only `if unseen`; `set` exclusive (L717-721) | one assignment per theatre; unseen guard | adv §2 "hard resync boss death via CLEU -> AUTHORITATIVE SET (both directions), not max"; adv §11 two-phase sync (engage + name → CLEU death → set) | STANDS | absolute set kept; `if unseen` guard not carried on the boss-set (unstated, not denied) |
| 14c | fractional stage 4.1; ordinal; duplicate costs meaning (L723-740) | fractions keep ordering total | ask C-5 "the ratchet compares stage NUMBERS (4.0 < 4.1 < 5); K-forward counts POSITIONS in the sequence" | STANDS | ported into the driver spec |
| 14d | No validation on authoring — a COURTESY; tell and trust, never refuse (L742-746) | match count tells; we do not refuse | adv §4 "`requires` is a static DAG -> checked at compile time on the desk (no cycles, every completor reachable, no orphans). Authoring mistakes die before a frame ever ticks"; adv §5 "Compile-time: DAG check, exactly-one-lure check" | CONTRADICTED | model: no refusal on authoring · adv: compile-time refusal of cycles/orphans/missing lure. Objects differ (stage duplicates vs DAG faults); posture differs |
| 15 | ★★★ RUN DATA IS HIDDEN. THE PRODUCT IS NEVER HIDDEN (L750) + beacon always on show (L774) + art-key trap (L792) + icon not identity (L799) + icon is a child's (L824) + icon result of character (L832) + tab 1 child roster (L888) | visibility/opacity/icon/rank rulings | — | UNTOUCHED | no arc material on map rendering or icons |
| 16a | ★★★ THE GAME HAS NO SHAPES — "the engine returns ONE scalar — a 3D distance… a `dz` we compute ourselves" (L903-916) | detection = rejection over the engine's one distance | brief Q2 "given d3D from the engine and a dz we compute"; ask H0-b "engine 3D ≤ R with a band gate is a lens, not a cylinder"; ask H0-b RULED "detection uses our OWN positions. The tracker's reading is for CALIBRATION"; adv R-a; ask H4 both terms from `GetCurrentPlayerPosition` | CONTRADICTED | model/brief: reject over the engine's scalar · ruling R-a: detection never reads the engine distance; both terms computed from own position. (The tracker distance survives as calibration: ask H8 max 1.9e-5) |
| 16b | "our rejection is the shape"; a shape costs what its test costs; the band is an extrapolation (L909-919) | shape = what we refuse | ask H4 POINT ~9 ops / SEGMENT ~30 ops, 1 div, no sqrt; res L203-206 op sequence mirrors H4 | STANDS | the rejection-rule framing is exactly H4; cost stated |
| 16c | radius / bandUp / bandDown magnitudes (L909) | (unstated numbers) | acc W3.2 band sourced; res L434-467 jump apex 1.64–1.74, character ~1.9–2.1 yd, min walked stack 0.50, "±2.5 stands"; ask H16 "the band is a TOLERANCE, not a model"; adv §3 arm zones default OPEN, satisfaction triggers default TIGHT | ANSWERED-BY-DATA | default band measured and ruled ±2.5; bandUp's purpose = protect a run-over from a second floor (§285) |
| 17a | ★★★ A BEACON POINTS TO SELF; go-to-Y = child at Y; direction crosses stages on ENTRY (L923-946) | no target-not-self; on-ramp y/n | adv §13 RULED "there are ONLY LURES — a beacon points to SELF… `A pointer <otherID>` leaves the instruction table"; adv R-d children may move the pointer; adv §13 activate: "that child points to itself" | STANDS | re-ruled in the arc's own vocabulary |
| 17b | exclusivity stops being a rule — one candidate at entry; chain inside a stage (L948-957) | exit never points, entry always does | adv §13 "at most one active lure per theater (last activation wins, as the tracker does); activation NEVER advances the stage"; adv §9 "a pointer never survives its stage - teardown on advance closes it"; adv §9 pointer `close: arrive | lead-in` | STANDS | handoff kept uncontested; arc adds an authored CLOSE and terminal release (acc W6) the model does not carry (B3) |
| 18a | ★★★ STAGE STATE IS THREE REGISTERS — set / ratchet / maxSeen (L961-982) | clip writes maxSeen, ratchet untouched | ask §K "Deferred BY DESIGN… maxSeen + escapement"; adv §2 forward listen: within K → jump; beyond K → ignored (no maxSeen register named) | OPEN | maxSeen not built (v1 = ratchet only); adv §2's K/jump is a different mechanism for the same hazard, not ruled against the registers |
| 18b | `set` is the ESCAPEMENT; recovery is authored (L984-993) | optional branch pulls you onto itself | ask §K deferred | OPEN | not built |
| 18c | OPEN: what a far-stage satisfaction should DO — "only the numbers can choose… the sprite… reports how often" (L997-999) | build-to-lookable | acc W5.2; res L224-244 false advances per K, per R, two fixtures; ask §I "far-stage/K — build-to-lookable via the walk (W5.2 already emits K=all vs K=3); ruling can wait" | ANSWERED-BY-DATA | the numbers the model asked for exist; the ruling remains open (ask §I) |
| 18d | OPEN: clipped node burned by its own `ifUnseen` (L1000-1002) | repeat guard, one node | adv §4 `once` "SPENT forever"; adv R-c spent-set INTERNAL | STANDS | same guard under a new name; hazard unchanged |
| 18e | driver needs a player way to CORRECT the index (L1004-1006) | requirement, not a design | ask C-1 "/reload… USER RECOVERY — scroll through the chains… a manual SEEK along the stages, which is also the shape of the model's 'player corrects the index' requirement — a control"; ask §K G6 route-remote control | STANDS | shaped as a route-remote seek control; not built |
| 18f | the system cannot tell a wrong advance from a right one (L1008-1011) | no independent signal | res W5.2 false-advance column; post §7 `hit` vs `stage`; acc W7.3 `false_advances` column | ANSWERED-BY-DATA | offline the walk can tell (against a recorded path); live it still cannot |
| 19a | ★★★ THE FLIGHT LIST — flatten is a TRANSFORMATION; step form; DENORMALISES two radii → two steps (L1015-1037) | source language → compiled list | adv §11 flatten contract "every default resolved… `requires` resolved to INDICES… DAG already checked"; adv §13 "Flatten = resolve IDs -> array indices: `E[i]`… `I[i]`… no parsing at runtime"; adv §13 RULED "two radii… two STEPS on one position (model: the flatten denormalises)" | STANDS | confirmed and detailed |
| 19b | every step self-contained → the driver STATELESS (L1039-1040) | no lookups, no references | ask H4 "Storage: previous position (3 doubles) + its mapID + a valid flag"; adv R-c spent-set internal; adv §4 `while` hysteresis state; adv §2 K window | SHRINKS | "stateless" narrows to "no reference resolution"; the driver holds prev-sample, spent-set, hysteresis, ratchet |
| 19c | list is not a program counter — standing conditions gated by STAGE; the world decides order within a stage; nested radii pace (L1042-1053) | sequence as distance | adv §4 satellites UNORDERED, `requires` only where asked; adv §2 listen = current + K (gate widened past one stage); adv §9 close radii | STANDS | order-by-world kept; the stage gate becomes a K window (adv §2) |
| 20a | ★★★ IDENTITY · CHARACTER · BEHAVIOUR — behaviour = sum of actions, no storage of its own (L1057-1069) | action tabs are the storage | adv §13 "Split what an entity IS (facts, held once) from what it DOES (instructions it self-reports against its ID)"; ENTITIES + INSTRUCTIONS tables | STANDS | same split as a data shape |
| 20b | specified vs exhibited set; the walk holds them against each other (L1071-1074) | timeline = exhibited | acc W5.3 timeline (stage, gt, cause); acc W7.1 | STANDS | built on desk |
| 20c | beacon with children has no behaviour of its own; a READ (L1076-1083) | theatre; children act | adv §3 theater lifecycle "enter -> children armed -> completor fires -> ALL torn down"; adv §13 `B1 theater … scene` row carries R and band | STANDS | the theater has an ARM ZONE (see 10a) but no actions of its own in adv §13's instruction table |
| 21a | ★★★ THE ADDRESS SHEETS — ID carries unique; label is a value; birth location intrinsic, NOT provenance; birth time rejected (L1087-1154) | identity read off a card | adv §13 "IDs unique PER ROUTE (the package is the namespace; merging = prefix-at-merge)"; adv §12 "every beacon CARRIES ITS OWN SEEDED xyz… (Provenance dataset/sample may ride along as an audit field; nothing depends on it)" | STANDS | ID scope named (per route); seeded xyz = birth location; provenance optional audit — consistent |
| 21b | RUN DATA NEEDS NO ID; time is the mint; `t:gt` pair (L1156-1182) | samples inert; t joins, gt measures | ask D1 schema t/gt; ask H6 reduced form `t, gt, x, y, z, mapID, floor` + combat, n; res L94-98 `gt` absent on satnav rows → `absentFields`, `t` reconstructed + `tSource` | STANDS | rows stay ID-less, keyed by time; arc adds absent-vs-defaulted discipline (B3) |
| 21c | A CHILD HAS NO STAGE, because it has a parent; `StageOf` (L1184-1195) | stage one hop away | adv §3 "Children live under its identity (4.1, 4.2 ...), inherit map/floor context"; adv §13 "order = activation edges… never 3.1/3.2. Ordinals are NOT outlawed" | CONTRADICTED | model: child has no stage; 4.1 is a BEACON insert (L723) · adv §3: children numbered 4.1/4.2 under the parent · adv §13: never 3.1/3.2 as a form. Two arc positions, one against the model (see B4-8) |
| 21d | sheets run against beacon and child — one gap (icon); `atX/atY` nil = never moved (L1197-1215) | data present | — | UNTOUCHED | — |
| 21e | face/meta split; prominence set by the reader; identity grammar footnote (L1217-1262) | presentation rules | — | UNTOUCHED | — |
| 21f | ⚠ the consumer does not exist yet — `Routes.BeaconAt` has no caller (L1264-1269) | sheets drawn over an unbuilt consumer | ask D2 "No run has ever been driven"; ask R3; ask §K "THE GAP IS SINGULAR: a CONSUMER. Nothing plays"; ask §I G1 open; res L512-513 `task_chain` "proves the pointing; it decides nothing hard" | STANDS | still true; a live chain probe (not a consumer) has run |
| 22 | ★★★ The map is the primary storytelling space — we give context, they derive meaning (L1278) | ships; not our storytelling | brief §5 "Emit, don't interpret. A number the user can look at is ours" | STANDS | brief re-states the posture; nothing on the map itself |
| 23 | ★★★ The control vocabulary — usage is a SUMMARY; act/response/outcome; label vs readout; not-a-usage (L1297-1369) | control taxonomy | — | UNTOUCHED | — |
| 24 | ★★★ Compactness and presentation are different jobs (L1373) | zone designation | — | UNTOUCHED | — |
| 25 | The three surfaces, three questions (L1398) — map/curation/promotion/object | four surfaces | ask §K G3 route remote "seventh surface" (from mvp_scope) | STANDS | table untouched; a surface with no row here is added by scope/arc (B3) |
| 26a | What is deliberately absent — No dungeon knowledge; no heatmap; no judgement of a route (L1417-1419) | absences are decisions | ask H10 §261 "A NAME is available, a GROUPING is not (§17)"; adv §11 same; ask H15 combat-model drift withdrawn; adv R-c; res L469-475 scope fence | STANDS | re-asserted after two named drifts |
| 26b | No consumer. "A route runner is a separate addon with its own remote" (L1421-1422) | recorder records; runner separate | adv §11 "The product is TWO addons… CONSUMER addon 2 - the driver"; ask §K G1 "THE DRIVER (Lua, in COA_DungeonRun)" | CONTRADICTED | model + adv §11: separate addon · ask §K G1: driver in COA_DungeonRun. Arc disagrees with itself (B4-7) |
| 26c | No capture-time filtering (L1420) | truthful view quietened downstream | ask H6 reduced corpus is a desk reduction; ask H6 bench note ghost cannot occur in-dungeon (0 of 5,295 legs) | STANDS | reduction happens at emit, not capture |
| 27 | Where the rest lives (L1426) | file map | (arc adds `driver_*` planning docs, `walk.py`, `landing/corpus/`) | UNTOUCHED | table not updated by the arc; new files listed in B3 |
| 28a | Hopes: ★★★ non-positional trigger `unit:death:<boss>` — third completion condition on the off-ramp (L1443-1461) | death names WHAT not WHERE | adv §11 CLEU two-phase sync (engage event `INSTANCE_ENCOUNTER_ENGAGE_UNIT` + `boss1..5` token poll → arm CLEU for the authored NAME → UNIT_DIED validates → SET); adv §13 entity kind `boss`; acc W5.3 cause `boss-set` testable on rfc_combat; res L267 "boss-set ABSENT, not zero" on SFK; res L422 "boss-set accounting balances" | STANDS | designed in detail; exercised offline on one fixture; engage also proposed as a trigger KIND for cues (B3) |
| 28b | Hopes: bosses = cheap `set:stage` correction; a RESYNC POINT (L1463-1474) | drift bounded between bosses | adv §2 "hard resync boss death via CLEU -> AUTHORITATIVE SET (both directions), not max"; ask §K far-stage "bounded by bosses" | STANDS | — |
| 28c | Hopes: ★★ A BEACON ARMS THE LISTENER; two sources validate (L1476-1495) — "THE BEACON ARMS IT" | position-scoped listener; position + name | adv §11 "**ARM ON ENGAGE, not on theater entry**… corrected here from my first wording"; two witnesses = engage-with-name + UNIT_DIED on that name; "Listener lives from engage until death / disengage" | CONTRADICTED | model: the beacon (position) arms the CLEU listener · adv §11: the ENGAGE EVENT arms it, not theater entry. Both keep "two witnesses"; the first witness differs (position vs engage) |
| 28d | Hopes: ★★ POSITION GATES THE LISTENING; a generous radius is FREE here (L1497-1508) | reach = condition, death = detect | adv §11 arm on engage (as above); adv §2 "listen range… gates the EXPENSIVE listeners (CLEU, note churn)" | CONTRADICTED | model: position gates CLEU · adv §11: engage gates CLEU; adv §2 still has a listen range gating "expensive listeners". Two arc statements (B4-11); the model's position gate is the contested one |
| 28e | Hopes: seven slots; third kind of text; four identity displays; gesture; what would make it real (L1514-1610) | vocabulary backlog | — | UNTOUCHED | — |

---

## B2. Model sections the arc NEVER touched (UNTOUCHED)

- 5b — announce edge (L262-266)
- 6c — focus drop-down · output-only sink · cursor/response split · hover IDs, click holds · three open questions (L292-420)
- 6d — sanitise at the boundary + the `|c |T |H` rendering hole (L337-352, L446-451)
- 6f — announce consented, improper by construction (L456-486)
- 7 — We are the product, not the thing authored inside one (L490)
- 8a — We read the game; the game does not notify us — capture arming (L511-529)
- 8d — arms on the authoring panes (L559-562)
- 9 — Two lanes, chosen at promotion (L566)
- 12 — Curation is DISTINCTION (L653)
- 15 — RUN DATA IS HIDDEN… and every icon / art-key / opacity / tab-1 subsection (L750-899)
- 21d — sheets run against beacon and child; `atX/atY` (L1197-1215)
- 21e — face/meta · prominence · identity grammar (L1217-1262)
- 23 — The control vocabulary (L1297-1369)
- 24 — Compactness and presentation (L1373)
- 27 — Where the rest lives (L1426)
- 28e — seven slots · third kind of text · four identity displays · gesture · what would make it real (L1514-1610)

---

## B3. Arc findings / designs with NO home in the model (new material)

| arc item | cite |
|---|---|
| Detection reads OWN positions; the tracker's distance is CALIBRATION + the arrow (R-a); in-flight pairing every tick | ask H0-b RULED; adv §0, §1 R-a; ask H8 |
| The calibration pair `|sd−od|` and the divergence detector / heartbeat (event + divergence, timer fallback ≤0.5 Hz) | ask H5; acc W2/W2.2; res W2.2b 57/57, ask H12 1,386/1,386 |
| The detection RULE in full: segment-vs-cylinder, point fallback, `usable()` (nil/non-finite), mapID straddle discard, no hold, clamp branch, gap bound (`dt`), teleport v_max ≥100 | ask H4, H13 A-1/A-2; acc W1.1–W1.10; res J1 |
| `once` (edge, segment) vs `while` (level, point + hysteresis R→R+margin) modes | adv §4; acc W1.8; res L190-199 |
| Forward-listen K (default 3), listen range, "quiet ≠ mute", off-route readout (Douglas-Peucker polyline baked at flatten) | adv §2, §11 |
| `requires` DAG (satellite / sequence / completor); any-of completors; compile-time checks | adv §4, §5 |
| ACTIVATE edges; "every node is DEAF until told to listen"; listen graph = positions + listen edges | adv §13 |
| Pointer CLOSE (`arrive` / `lead-in`), teardown on advance; the pointer is a HEADING; zig-zag readout | adv §9, §10 |
| Terminal RELEASE required (marker never self-clears); `ts=0` at clear; overwrite instantaneous; a 5 yd trigger fires at 4.0–4.8 | acc W6; res L479-513; ask C-3 |
| Give-back / reclaim (F-ii) as a product ruling owed | ask F-ii, §I; adv §8, §9 |
| Three-tier fence DEV DESK / EDITOR / CONSUMER; product = two addons; per-tick budget by kind (geometry / CLEU / API) | adv §11 |
| Package format: DATASET economy · ROUTE PACKAGE (meta / flat / source) · schema_version · integrity hash · reimport-diff-clean · seed once, carry forever, re-seat | adv §12 |
| Entity table + instructions (A / Q); IDs unique per route; flatten = arrays `E[i]` / `I[i]` | adv §13 |
| Shipping constants: design speed 7.0 yd/s (p50; mounted rare by construction), capture 1 Hz (2.41 yd max; 2 s not adequate), band ±2.5 tolerance, ~~POLL_MIN 0.2 / POLL_MAX 2.0 / MAX_CLOSING_SPEED 30 (exceeded 50.6 in pulls; inert)~~ ⚠⚠ **SUPERSEDED 2026-08-20 (RI-34): POLL_MIN 0.1 / MAX_CLOSING_SPEED 100.** ★★ And read the struck text again — *"exceeded 50.6 in pulls; **inert**"*. **The evidence for RI-34 was already on disk, already measured, and marked harmless.** It WAS harmless under segment, where over-running the divisor costs a late arrival notice; under point + band + gate (A11.2a) the same over-run is a MISSED BEACON. ⚠ Nothing in this line's text changed when the rule narrowed — *a grep finds moved words, it cannot find moved load* — so the note was true when written and became false without being touched | ask A1, H8, H9, H16; acc W3, W3.2, W4; res L387-392 |
| Throttler paced on the current beacon's R (`slack = (dist − R)/MAX_CLOSING_SPEED`) | ask C-2 |
| Capture change: per-stage `pin` trace (set/arrive/clear events) so the walk can replay pointing | ask C-4; acc W6.2; res L515-520 |
| Route remote (arm/go/stop/report) and USER RECOVERY = manual seek along the chain; `/reload` ruled out as a driver concern | ask C-1, §K G3/G6 |
| Corpus/emitter discipline: `absentFields`, `conditionalAbsent`, `variance_warnings()`; fields placed by kind | res L94-114; post §10 |
| Tracker state `ts` characterised (0 declined · 2 tracking · 4 ≤5.5 yd comparator); verifier only, never a mechanism | ask H11; post §8, §9; ask C-6 |
| Physical measurements: jump apex 1.64–1.74, character height ~1.9–2.1, z datum = base point, mount 17.5 yd/s, `UnitPosition` absent | res L434-443 |
| Combat-shaped readouts (gaps = 24 % of run; lure/destination split 6/6; boss `while` radii rMAX 17.4/29.4; W4 split 2.2 gap / 4.95 pull) — withdrawn as CRITERIA, retained as author-side readouts | res L305-401; ask H14, H15; acc W5 |
| Engage as a trigger KIND for cues ("<name> engaged → note") | adv §11 |
| Bands by JOB: arm zones default OPEN, satisfaction triggers default TIGHT | adv §3 |
| Route-package provenance chain (origin → tweaker) in `meta` | adv §12 |
| New files: `driver_analysis_*.md`, `driver_design_advisory.md`, `driver_walk_*.md`, `driver_posture.md`, `addons/tools/walk.py`, `addons/landing/corpus/`, `emit_chain_route.py`, `read_tracker_state.py` | acc W0; res L6-12, L310, L481 |

---

## B4. Arc documents disagreeing with EACH OTHER

| # | side A | side B | state |
|---|---|---|---|
| 1 | brief §2 "GetSuperTrackedPosition's distance is engine 3D yards… **Never compute your own**"; brief §6 stop "designing a distance function from scratch" | ask H0-b RULED "detection uses our OWN positions"; adv R-a | superseded by ruling; brief not amended |
| 2 | acc W5 two-rates note (original) "bound the live driver from the PESSIMISTIC side" | res L272-296 R=1 coarser detects MORE (13 vs 12); post §5 "NOT symmetric… one-directional" ; ask H13 A-4 "symmetric bound"; ask H14 "the same content" | acc W5 rewritten (L159-169); post §5 wording still "not symmetric" |
| 3 | acc W4 (original) "nearest segment" | res M2 (L47-67) bracketing reproduces all twelve; global none | acc W4 amended (L119-124) |
| 4 | post §3 "the segment advantage does NOT vanish by R=5" (RFC fixtures) | res L403-407 "Retracted entirely" (bridging 43/69/125 s combat holes); ask H14 "posture §3 retraction = A-2 vindicated"; res W1.5 L186 "vanishes by R=5" | posture §3 text still stands as written; retracted elsewhere |
| 5 | post §11 "mapX/mapY dropped, so C1 was uncheckable" | ask H13 A-6 "likely FALSE… re-proven on every emit"; post §11 restated | resolved in posture (struck) |
| 6 | post §10 constant-in-run → header rule "invented" | ask H13 A-5 rule should be a CHECK not the placer; post §10 restated (rule was audit; `variance_warnings()` added) | resolved in posture (struck) |
| 7 | adv §11 "The product is TWO addons… CONSUMER addon 2 - the driver"; (model L1421 agrees) | ask §K G1 "THE DRIVER (Lua, in COA_DungeonRun)" | unresolved |
| 8 | adv §3 "Children live under its identity (4.1, 4.2 ...)" | adv §13 "order = activation edges… **never 3.1/3.2**. Ordinals are NOT outlawed… but a PATTERN drawn as an activation chain" | same document; §3 not amended |
| 9 | adv §13 first table `C1 A pointer C2 close=lead-in`; adv R-d "Children may move the pointer"; adv R-e "**Exactly one** lure per theater" | adv §13 RULED "ONLY LURES… `A pointer <otherID>` leaves the instruction table… `A activate <childID>` replaces"; "at most one **active** lure per theater… activation hands the arrow" | same section; table left as first written; R-e "exactly one" vs "active lure moves" |
| 10 | adv §12 L289-291 "A route without positional data has no meaning in the editor… so **the dataset is part of what travels**" | adv §12 L308-334 "SEED ONCE, CARRY FOREVER… Once seeded, the route needs no dataset… a route imported with no dataset held is fully tweakable and fully runnable" | same section; first paragraph predates the ruling |
| 11 | adv §2 "listen range… the range gates the EXPENSIVE listeners (CLEU, note churn)" (position gate) | adv §11 "**ARM ON ENGAGE, not on theater entry**" (event gate) | same document; §2 not amended |
| 12 | res W1.2/W1.3 L158 "a same-map segment bridges a 40 yd gap and fires" PASS | ask H13 A-2 "the hazard PASS"; acc W1.10 "the 40 yd fixture flips from PASS to 'must NOT bridge'" | resolved: W1.10 added; res L417 W1 PASS all ten |
| 13 | acc W3.2 "shipped default = a value that **admits the p99 jump** and rejects the smallest floor separation" | res L445-467 "`bandUp` does not need to admit a jump… ±2.5 stands"; ask H16 "the band is a TOLERANCE, not a model… My W3.2 wording leaned toward measuring the jump precisely" | acc W3.2 text not amended |
| 14 | acc W0 "The walk is the desk **simulator**"; acc W6 (original) chain probe "set A → walk → release → set B"; ask H9 "live CHAIN PROBE… acceptance W6" | ask H16 "The product is a CONDITIONAL STATE MACHINE, not a simulation… my W0 word 'simulator'… withdrawn… I built W6 too large; the Bench shrank it (§288)"; acc W6 L185-189 reduced | W0 wording still "simulator"; W6 amended |
| 15 | ask §K G1 driver step "finish: RELEASE (Beacon.Clear; **required**, C-1)"; acc W6 "Release is a REQUIREMENT, not a courtesy" (driver-side) | ask C-1 "clear is a CONDITION in the instruction set, **not a driver rule**… the driver has no 'finish' behaviour — it executes an instruction whose response is *clear the pointer*" | both in asklist; G1 phrases release as a driver step, C-1 as an authored instruction |
| 16 | ask H1 request "≥1 deliberate boundary cross while pinned inside" | ask bench note on H1 "the boundary cross is not available in-instance… the request halves" | resolved in-place |
| 17 | ask H3 "1 Hz… **marginal** at R≈5 through turns" | ask H8 "1 Hz ADEQUATE for R=5… my 'marginal through turns' was pessimistic" | self-corrected |
| 18 | ask H14 Ruling 2 "THREE sources, each testing a different thing" (kills / combat-END lures / authored pins) | ask H15 "Three-source framing withdrawn as a criterion structure"; acc W5 L138-147 WITHDRAWN | self-corrected; H14 text stands as history |
| 19 | ask §I "A2 mounted speed — **unmeasured**, low priority" | res L443 "mount jump 1.5726 · **top speed 17.50 yd/s**" | ledger not updated |
| 20 | acc W6 (before §290) "W6.1 overwrite… ten seconds to confirm"; res L423 acceptance state "W6 NOT STARTED" (at §285) | acc W6 header "DONE 2026-08-17 (§290)"; res L479 "W6 — DONE" | time-ordered; both texts remain in the same files |
| 21 | brief §4 Q2 "how [the band] combines with the distance is not [ruled]"; ask B1 "SHAPE ruled; EVALUATION does not exist… a proposal to specify" | brief §6 stop "deciding the reach SHAPE is open" | consistent on shape, but brief §2 table row "Reach is a vertical cylinder" is stated as fact while ask B1 says the cylinder is a proposal against a "lens" (H0-b) — evaluation form differed until R-a |

---

## Counts (B1 rows; split rows counted individually — 78 rows)

    STANDS            37   1a 1b 2c 3a 4a 4h 5a 6a 6b 6e 8b 8c 10b 10d 11 13 14b 14c 16b 17a 17b
                           18d 18e 19a 19c 20a 20b 20c 21a 21b 21f 22 25 26a 26c 28a 28b
    SHRINKS            3   4e 4g 19b
    ANSWERED-BY-DATA   7   2b 3c 4b 14a 16c 18c 18f
    CONTRADICTED      11   3b 4c 4f 10a 10c 14d 16a 21c 26b 28c 28d
    OPEN               4   2a 4d 18a 18b
    UNTOUCHED         16   5b 6c 6d 6f 7 8a 8d 9 12 15 21d 21e 23 24 27 28e
