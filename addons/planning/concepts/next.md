# CONCEPT HOME · `Next` — what I do when my tabs are complete

_A HOME is an INDEX, never a second copy (AL-26, Battlewrath 2026-08-22: "a home is better than a
run-time cost — it's greppable and inspectable"). It says what the concept IS in a few lines, its closed
list, and POINTS at every place that rules or grades it. The pointed-at documents stay authoritative;
if this page and one of them disagree, the document is right and this page has drifted. Checkable: a
home must name every governing document the vocabulary appears in (`emit_divergence` computes that set)._

## WHAT IT IS
A NODE's characteristic (character layer — mutable): what happens when ALL the node's action tabs are
complete. ONE field, `Next(Type, arg)`. It is never a tab (a tab fires on its sense; Next fires on
completion — the logic hole of AL-9/A2.9). The old pane's `role` + `setStage` is its replaced spelling.

## THE CLOSED LIST
    Step      → the ordinal cursor moves to the next positive ordinal (the constant; default for an ordinalled node)
    Stage     → the NEXT STAGE PRESENT in the route (never +1: gaps are legal)
    Set(N)    → stage = max(current, N) — never regresses (the ratchet's rule applied to recovery)
    absent    → NO OUTCOME, DERIVED from position: ordinalled → Step · zero node → nothing follows.
                Never a fourth word; never a degenerate Set.
    THE OFFER follows what exists: greater ordinal → Step · Stage · Set; last step / childless → Stage · Set.

## WHERE IT IS RULED (read these; this page only points)
    driver_architecture.md   §4b (order of effects step 5; the no-outcome block; AL-21/AL-24 sentences) · §4d · §5 L17
    driver_programmatic_model.md   §2 NEXT block (A2.9's hole and fix) · the offer · "where the constant lives"
    driver_authoring_acceptance.md A2.7 (all tabs) · A2.8 (told or dry) · A2.9 (Next, the offer, recovery default)
    driver_data_model.md     row 12 (ONE field) · the characteristic record
    driver_manager_acceptance.md   A12.5 (complete — the ledger fires Next) · A12.7 (recover) · the tray-0 rows
    driver_ui_acceptance.md  A10.3a (the NEXT dropdown in the character group, with its field)
    ARCHITECT_LOG.md         AL-2 (the manager fires it) · AL-9 (not a tab) · AL-18 (the seed) · AL-21 (a store field;
                             role migrates; no-outcome) · AL-22/23 (Next is a latch per arming) · AL-24 (no fourth type)
    Reconcile_inbox.md       RI-49 (drained) · §479 (the no-outcome landing)
    contract.lua             `nextType` / `nextArg` declared on the characteristic record
    adaptor                  `ratchet` → the labels Next step · Next stage · Set stage N

## WHAT IS OWED — derive it; never read it here (`emit_built_state.py`, once its one bad cite is fixed)
As of 2026-08-22 by hand: the store fields (`Routes.NEXT_TYPES`, `SetNext`) and `NodeDone`'s branch are BUILT;
the PICKER (RI-69) and the `role`/`setStage` migration remain. ⚠ A hand line; it rots — the tool is the truth.
