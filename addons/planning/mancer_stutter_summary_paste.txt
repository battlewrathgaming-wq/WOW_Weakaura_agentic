**Method:** 131-second sample in a capital, one sample per second, using the client's own CPU profiler (`/console scriptProfile 1` + reload). The numbers are the engine's per-addon accounting — I only sampled it.

**Totals over the window:**
```
Libellus Leti    3,111 ms CPU    (mean 23.7 ms/sec)
TurboPlates              0 ms
MancerLedger             0 ms
my own sampler        12.6 ms    (total, whole run)

framerate:  min 31   mean 91   median 96
```

**Bursts:** 17 of 130 seconds went above 40 ms, arriving a median 8 seconds apart, peaking at 108 ms in a single second.
Mean framerate in a second containing a burst: **55**. In seconds without one: **97**.

**The 8 worst framerate seconds, each shown with that second's Libellus CPU:**
```
t+14s    fps 31.0     58.4 ms
t+30s    fps 38.5     53.9 ms
t+38s    fps 35.4    100.3 ms
t+46s    fps 32.1     92.0 ms
t+77s    fps 35.1     84.0 ms
t+92s    fps 39.5     76.8 ms
t+107s   fps 31.7     48.1 ms
t+130s   fps 31.1     47.7 ms
```
Every other addon read 0.0 ms in those same seconds. For scale, at ~90 fps one frame is about 11 ms — so a 100 ms second is roughly nine frames of budget spent at once.

**Limits, so it isn't over-read:** this identifies the addon and the second — not the function, and not individual frames. `GetAddOnCPUUsage` attributes per addon, and my sampler runs at 1 Hz. The nameplate path I flagged earlier came from reading the source, not from these numbers — separate evidence, worth checking separately.

Raw per-second data attached: 130 rows, framerate plus CPU for all four watched addons — plot it yourself rather than taking my summary.

Reproducible without my tooling: `/console scriptProfile 1`, reload, play a minute in a city, then run `/run UpdateAddOnCPUUsage() print(GetAddOnCPUUsage("LibellusLeti"))` twice sixty seconds apart — the difference is CPU ms in that minute. `/console scriptProfile 0` + reload turns it off.
