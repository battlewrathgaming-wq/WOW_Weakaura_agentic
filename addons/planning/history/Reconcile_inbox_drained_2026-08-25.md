# Reconcile inbox — DRAINED 2026-08-25

_The working-out. Conclusions live in `ANALYST_LOG.md`._

---

## RI-77 DRAINED (Analyst, 2026-08-25) · SPENT — the UI seat fixed the tool, and **my premise was wrong**

_Outcome: nothing to do. Their answer stands unedited above the original; this stamps it._

**I filed:** `check_sheet.py` exits 2, configuration 12 cannot be expressed, *"the cause is capture
data, not the tool"* — and asked whether to skip the capture or drop it.

**They answered:** it was **the tool.** `derive_quantum` tested its candidate with an **ABSOLUTE**
tolerance, so a configuration whose widths are larger failed on SIZE ALONE. Relative now (UL-17),
and config 12 reads `275/275 on the grid`. ⟶ **Neither branch of my question needed answering** —
nothing to skip, nothing to drop. *"A capture that could not be expressed by a broken deriver was
never a bad capture."*

⚠⚠ **THE LESSON IS MINE AND IT IS EXACT.** I stash-tested the edit and proved *"exit 2 predates my
change"* — TRUE — then wrote *"the cause is capture data, not the tool."* **The measurement proved
NOT-MY-EDIT and I extended it into A CAUSE.** [[dont-extend-past-the-evidence]], on the same day I
carried that memory in the band. ★ The honest filing was one clause shorter: *exit 2, pre-existing,
cause unknown.*

★ What the item got right, kept because they said so: *"a checker that exits 2 every run is a
checker whose red carries no information."* The inert-guard shape from the other side, and the
reason it was worth filing even though the diagnosis missed.
⟶ And their finding is bigger than mine was: **the same absolute-tolerance fault in three tools
that week** (§578's `--wrap`, `derive_quantum`, and `==` on a scaled float in `task_sheet.lua`).

---

## RI-78 DRAINED (Analyst, 2026-08-25) · the register half — three sites stamped, the LIGHT version reserved

_Outcome: `interface/map.md` ×3, DR_Content_20's form. The code half was already done and cites AL-59 in place._

    the model line      the sentence struck, with the stamp and the reserved home beneath it
    the gesture table   the row struck, pointing at the model
    the `opens` list    the clause struck, dated

**STAMPED (DR_Content_20's form):** `right-click → Object pane spawn` · retired **2026-08-25** · **AL-59**.
⟶ **RESERVED, unspecified:** a LIGHT version replaces it — a named home, not a spec (A10.8's
pattern). What right-click does meanwhile is the bench's to leave clean.

⚠⚠ **AND WHAT IS *NOT* RETIRED, said out loud because a stamp invites over-reading.** Right-click
OWNERSHIP stands (§34's boundary), and **right-click PANNING in `map_controls.md` is untouched** —
a different gesture that merely shares a button. ★ §69's other two gestures stand: hover reads,
left click selects and pins.

★ The code's own note is the best statement of why it went WHOLE: *"`RightButtonUp` is no longer
REGISTERED, rather than registered and ignored: **a gesture that fires a handler which does nothing
is not retired, it is hidden.**"*

---

## RI-80 DRAINED (Analyst, 2026-08-25) · two cleanups and the assurance — **the arc's last open edge is closed**

_Outcome: `driver_ui_acceptance.md` · `interface/map.md` · the 22-law walk. `ANALYST_LOG` 2026-08-25._

**1 · `args.run` → `args.curate`.** Exactly the two sites the item predicted (A10.1a's model line
and its `TEST:` line). The code renamed at AL-56 and cites it at `options.lua:119`; the row was
NAMED as riding the rename and did not. ★ **The other two lanes were always right** — `promote` and
`node` match — so the drift was ONE word of three, **which is the kind that reads as fine.**

**2 · RI-78's register half.** Done and drained separately; see RI-78 above.

**3 · ⟶ THE ASSURANCE — the law pass's self-named open check, CLOSED.**
The pass ended: *it did NOT verify that each law's cited home still says what the law says.*

    22 of 22 laws walked · every cited home RESOLVES · every home read CARRIES its law

★★ **AND THE MECHANICAL HALF NEARLY REPORTED TEN FALSE DRIFTS.** My resolver flagged 10 of 22, and
**every one was the resolver, not the record**: `§3a`/`§4b`/`§4d` are `###` subsections and it only
read `##`; `A11.3`/`A11.4`/`A11.9` are FAMILIES whose members exist; `RI-4`/`RI-15` live in
`history/` under an indented heading. ⟶ **A resolver that is too strict manufactures drift exactly
as a sweep that is too loose manufactures collisions** — the same fault, opposite sign, and this
week produced one of each.

**What was READ, not merely resolved:**

    DR_UI_21 -> §4d      ✅ AL-61's move LANDED - the forced authoring order and the
                            MEANING/SUBJECT boundary are both there, stamped, pointing back at §5
    DR_UI_22 -> §4d      ✅ the whens · A10.9
    DR_Content_17 -> §4b ✅ the latch (AL-23's two latches, per tab and per node)
    DR_Content_15 -> A11.2a ✅ *"the gate: same mapID, tested FIRST"* IS the law
    DR_Process_18 -> §7  ✅ *"Concept HOMES (AL-26): one short page per load-bearing concept"*
    DR_Process_19 -> §7  ✅ the checker-derives-it clause
    DR_Content_20 -> the checkers ✅ `check_retired.py`: *"a retirement is not done until its
                            identifier has been grepped"* - the stamping half, enforced

⚠ **THE HONEST EDGE:** `DR_Content_1..14` cite files and use-case sections rather than a `home:`.
Their cites RESOLVE and were spot-read, not walked line by line. **Named rather than counted as
covered** — that is the same sentence the law pass closed with, and it is smaller now than it was.
